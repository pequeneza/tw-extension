---
name: User role and technical profile
description: Developer of xBot Chrome extension for TribalWars PT — experienced with both React overlay and vanilla JS userscript injection into game pages
type: user
---

The user is the sole developer of the xBot Chrome MV3 extension for TribalWars PT. They work across two distinct UI layers: the React/TypeScript overlay (Shadow DOM, Vite build) and vanilla JS userscripts injected directly into game pages. They are comfortable with both layers and expect review feedback to be specific, justified, and applied directly rather than suggested as options.
